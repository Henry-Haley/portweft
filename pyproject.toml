[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "portweft"
version = "0.1.0"
description = "Lightweight Nmap XML orchestration for service facts."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
impacket = ["impacket>=0.11"]

[project.scripts]
portweft = "portweft.cli:main"

[tool.setuptools]
packages = ["portweft"]
